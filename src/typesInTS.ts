 // type inotation and type inference

//  (type inference) is when we explicitly define the type of a variable, function parameter, or return value. For example:
//  ```typescript

let drink = "coffee"; // TypeScript infers the type of drink as string
// drink = 2; // Error: Type 'number' is not assignable to type 'string' and same as interger or other data types


// type inotaion is when we explicitly define the type of a variable, function parameter, or return value. For example:
let age: number = 25;