1. npm init -y // initialisatin for the typescript project
2. npm i -D typescript // initialze the typescript globally
3. npx tsc --init // for compilation (and) change tsconfig.json file

4. npx tsc // create the mapping
5. node dist/index.js  // to run the file